# 首版完成度与待验收项

更新：2026-09-23。`main` 已包含三个独立任务分支的实现及集成代码，是**可演示的 Windows 工程 MVP**。它尚未完成现场产品验收，也不是包含 SDK、模型权重和实测素材的免安装发行包。这里区分“功能已接通”和“产品指标已验收”；后者不能由模拟事件、标定拟合或单次演示替代。

| 实施阶段 | 当前状态 | 尚需完成 |
|---|---|---|
| 1. 接入与能力诊断 | Windows 一键启动、原生 X4 Air 采集、960×480 实时 ERP、AAC 解码、相机/电脑麦克风来源显示均已工作。相机内置麦克风的强降风噪模式实测同源双声道；全景声模式实测 48kHz 四声道，四方位及转动标定通过。 | 弱降风噪、立体声与全景声的同条件声音质量对照；真实 USB 拔插后的无人操作恢复验收。 |
| 2. 可观看闭环 | 本地服务、两个 WebSocket、全屏第一视角 HUD 与双栏调试已接通；真实相机约 30 FPS。集成期间运行的相机服务完成 602 秒连续采集：299/299 次有效，FPS 中位数 30.3；此前的三次起停与采集进程故障注入也已通过。画面失效不会自动变成无标记回放。 | 最新提交尚未重启相机服务做长时复测；真实 USB 拔插后的无人操作恢复尚未验收；多机型/更多电脑不在首版已验证范围。 |
| 3. 算法闭环 | 本地 YAMNet 输出 10 组声音类别，YOLOv8n/DirectML 输出视觉候选；全景声粗方位、低置信度降级、类别/时间匹配、事件优先级与过期清除已实现。真实人声、相机音频方位和 HUD 图标/声纹已联调。 | 鸣笛、警笛、铃声等危险类别在真实现场的召回/误报；不同背景声、多声源、运动和风噪下的方向覆盖率及错误率。现有标定平均 9.8°、最大 16.0°只描述标定集。 |
| 4. 演示与验证 | 最近事件、录制回放、模拟 UI、报告导出、安装/构建/启动脚本已具备。115 项自动测试通过；40 种 HUD 布局的中央区域文本/卡片无遮挡比例最小 80.86%。可恢复的现场试次 CLI、只读录音延迟诊断、可校验的本地源码 ZIP 均已完成。 | 240 次独立正样本与 10 分钟安静负样本尚未执行；真实发声到 HUD 的 P95 延迟尚未测量；对外发布前仍须确定项目许可证及第三方分发边界。 |

因此，**工程端到端链路已可演示，但四项产品数值目标（危险声召回率、独立现场方位误差、发声到 HUD P95、安静误报率）均未得到完整现场验收**。模型单次推理约数毫秒不等于发声到显示的延迟。此前方向采样包含用于确定声道映射的标定集，不能再充作独立准确率样本。

## 原计划验收场景的实际覆盖

| 场景 | 当前证据 | 尚缺什么 |
|---|---|---|
| 10 分钟连续运行 | 集成期间已启动的真实相机服务运行 602 秒，299/299 次状态采样有效；FPS 中位数 30.3。 | 最新 `main` 提交重启后的同等长时实机复测。 |
| 重复起停三次、录制回放 | 实机三次起停、真实音视频录制回放均通过。 | 更长时间和不同电脑上的重复性未验证。 |
| 断线与首次无帧 | 采集进程被强制结束后自动恢复通过；启动无帧的重建逻辑已有代码。 | 真实 USB 拔插后的无人操作恢复，以及受控“启动 5 秒无有效帧”的端到端实机复测。此前拔插后是人工通过 API 重启恢复。 |
| 模型加载失败、无相机音频 | 模型缺失有自动测试；相机无音频故障注入时，真实电脑麦克风备用路径通过。 | 模型缺失的完整浏览器故障呈现、真实设备持续无音频场景未单独做实机验收。 |
| 角度、拼接缝、抢占与过期 | 圆周角/扇区、拼接缝去重、P0 抢占和过期清除有自动测试；八方向 HUD 仅做模拟 UI 检查。 | 真实跨拼接缝目标、行进时转头及多声源下的完整实景验证。 |
| 视觉检测与算力回退 | 本机 DirectML 模型已在实景检出人员；CPU 提供程序回退逻辑已实现。 | 车辆/自行车等目标的独立带标签实景检出率、真实跨缝跟踪、强制 DirectML 不可用后的 CPU 回退实测。 |
| 两种屏幕分辨率与中央面积 | 1920×1080、1366×768 的实景浏览器截图和 40 种模拟布局检查通过；中央区域最小无遮挡比例 80.86%。 | 现场强光、复杂背景下的文字可读性和半透明光弧感受未量化。 |
| 声音类型、方位与真实告警 | 实机识别人声并显示声纹/图标；四方位与转动标定通过。 | 鸣笛、警笛、喊声 240 次独立矩阵为 0/240；铃声等其他类别亦缺带标签实测。不同风噪模式、多声源与运动下效果未验收。 |
| 10 分钟安静负样本、发声到 HUD 延迟 | 离线录音探针测得模型及事件/WebSocket 的代理耗时。 | 安静负样本 0/10 分钟；真实发声起点与 HUD 首次可见须用同一外部时钟/录像记录，尚无 P95。 |
| 安装与分发 | 当前 Windows 笔记本的一键启动及本地源码 ZIP 哈希校验通过。 | 全新 Windows 电脑的从零安装、SDK/模型获取、构建及首次标定流程尚未端到端验收。 |

首版方案明确排除了真实语音转录、Quest、实体震动、云端大模型、账号和精确距离/TTC；这些不是当前 MVP 的遗漏。对外称为“开源项目”或发布可再分发包之前，还需决定项目根许可证并核对第三方 SDK/权重条款。

并行分支从已测试的集成基线 `a917479` 分出；互相独立的工作树位于本机 `.worktrees/`。三项已分别提交，先合入 `codex/mvp-integration`，再快进合入并推送到 GitHub `main`；任务分支保留供审查：

| 分支 | 范围 | 可验证产物 |
|---|---|---|
| `codex/latency-telemetry`（`e44491a`） | 区分模型耗时与回放链路耗时，保留真实发声到 HUD 指标的现场边界 | [离线诊断](latency-diagnostics.md)、测试、延迟字段；集成提交 `a6698d4` |
| `codex/field-trials`（`b04ee87`） | 让 240 次现场试验可恢复、可校验地逐项填写 | [现场流程](field-testing.md)、CLI、测试；集成提交 `0902eb0` |
| `codex/source-release`（`3dd96d7`） | 建立排除 SDK、权重和个人录制素材的源码包检查 | [源码包说明](release.md)、哈希清单、测试；集成提交 `7d44c2c` |

集成后的完整测试为 115 项通过。现场工作表仍为 0/240，安静负样本为 0/10 分钟。对 9.984 秒已有真实人声录音，离线回放诊断测得模型推理 P95 7.75 ms、最后 PCM 块时间戳到事件 P95 22.81 ms、到本地 WebSocket 收件 P95 122.02 ms（8/8 次事件更新收到）。这些数值不含物理发声起点、相机采集和浏览器绘制，**不能代替发声到 HUD 的 P95 验收**。现场声音摆放、标注与外部同钟录像仍需操作者配合。
